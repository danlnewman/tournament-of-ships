@page "/ship/{text}"
@using server.Data
@inject ShipService ShipService

<h1>You successfully sent @Text</h1>

@code {
    [Parameter]
    public string Text { get; set; }

    protected override void OnInitialized()
    {
        Text = Text ?? "none";
        ShipService.SendDirection("test", Text);
    }
}